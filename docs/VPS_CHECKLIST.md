# ✅ Чек-лист для деплоя на VPS

## 🔍 Проверка перед деплоем

### 1. Файлы проекта
- [x] Структура проекта правильная (`src/`, `data/`, `config/`, `docker/`)
- [x] Dockerfile использует `selenium/standalone-chrome` (оптимизирован для VPS)
- [x] Docker Compose настроен правильно
- [x] `.env.example` создан для документации
- [x] Все пути относительные (работают на любом сервере)

### 2. Переменные окружения
- [x] `BOT_TOKEN` загружается из переменных окружения
- [x] Нет хардкода токенов в коде
- [x] `.env.example` содержит примеры

### 3. Docker конфигурация
- [x] Dockerfile использует правильный базовый образ
- [x] Права доступа настроены (`user: "1000:1000"`)
- [x] Volumes правильно смонтированы
- [x] Healthcheck настроен
- [x] Restart policy установлен (`unless-stopped`)

### 4. Сетевая конфигурация
- [x] Метрики слушают на `0.0.0.0:8000` (доступны извне)
- [x] Порты правильно проброшены в docker-compose
- [x] Сеть создана для изоляции сервисов

### 5. Безопасность
- [x] Токен не в коде
- [x] Права доступа к файлам настроены
- [x] Пользователь в контейнере не root (seluser)

### 6. Производительность
- [x] Selenium в headless режиме
- [x] Скриншоты только при ошибках
- [x] Кеш геокодинга работает
- [x] Метрики Prometheus настроены

### 7. Мониторинг
- [x] Healthcheck в Dockerfile
- [x] Метрики Prometheus
- [x] Логирование ошибок

## 🚀 Команды для деплоя

### На VPS сервере:

```bash
# 1. Создать директорию
mkdir -p /opt/maptrack && cd /opt/maptrack

# 2. Загрузить проект (через git или scp)
git clone <your-repo> .
# или
scp -r MapTrack/* root@VPS_IP:/opt/maptrack/

# 3. Создать .env файл
cp .env.example .env
nano .env  # Добавить BOT_TOKEN

# 4. Запустить
cd config
docker-compose up -d --build

# 5. Проверить логи
docker-compose logs -f bot

# 6. Проверить статус
docker-compose ps
```

## 🔧 Настройка файрвола (если используется UFW)

```bash
# Разрешить SSH
ufw allow ssh

# Разрешить порты для сервисов
ufw allow 8000/tcp  # Метрики
ufw allow 9090/tcp  # Prometheus (опционально)
ufw allow 3000/tcp  # Grafana (опционально)

# Включить файрвол
ufw enable
```

## 📊 Проверка работы

1. **Проверка бота:**
   ```bash
   docker-compose logs bot | grep "Бот запущен"
   ```

2. **Проверка метрик:**
   ```bash
   curl http://localhost:8000/metrics
   ```

3. **Проверка контейнеров:**
   ```bash
   docker-compose ps
   ```

4. **Проверка здоровья:**
   ```bash
   docker inspect maptrack_bot | grep Health
   ```

## ⚠️ Важные замечания

1. **Права доступа:** Убедитесь, что пользователь на VPS имеет права на запись в `data/` и `screenshots/`
2. **Диск:** Проверьте свободное место (минимум 2GB для Docker образов)
3. **Память:** Рекомендуется минимум 1GB RAM для работы Selenium
4. **Сеть:** Убедитесь, что порты не заблокированы файрволом

## 🆘 Решение проблем

### Бот не запускается
```bash
docker-compose logs bot
# Проверьте BOT_TOKEN в .env
```

### Ошибки прав доступа
```bash
sudo chown -R $USER:$USER data/ screenshots/
```

### Контейнер падает
```bash
docker-compose restart bot
docker-compose logs -f bot
```

### Метрики недоступны
```bash
# Проверьте, что порт 8000 открыт
netstat -tulpn | grep 8000
```



