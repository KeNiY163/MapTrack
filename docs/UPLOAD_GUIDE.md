# 📤 Загрузка файлов на VPS через SCP/SFTP

## 🎯 Быстрый способ (рекомендуется)

### Шаг 1: Подготовка архива на локальном компьютере

**Windows (PowerShell):**
```powershell
# Перейдите в папку с проектом
cd C:\Users\misha\Desktop\MapTrack

# Создайте архив (требуется 7-Zip или WinRAR)
# Или используйте встроенный архиватор Windows
Compress-Archive -Path * -DestinationPath maptrack.zip
```

**Linux/macOS:**
```bash
cd /path/to/MapTrack
tar -czf maptrack.tar.gz *
```

### Шаг 2: Загрузка на VPS

**Через SCP:**
```bash
# Windows (если установлен OpenSSH)
scp maptrack.zip root@YOUR_VPS_IP:/tmp/

# Linux/macOS
scp maptrack.tar.gz root@YOUR_VPS_IP:/tmp/
```

**Через SFTP (FileZilla, WinSCP):**
1. Подключитесь к VPS через SFTP
2. Загрузите архив в папку `/tmp/`

### Шаг 3: Распаковка на VPS
```bash
# Подключение к VPS
ssh root@YOUR_VPS_IP

# Создание директории
mkdir -p /opt/maptrack
cd /opt/maptrack

# Распаковка
unzip /tmp/maptrack.zip
# или
tar -xzf /tmp/maptrack.tar.gz

# Удаление архива
rm /tmp/maptrack.*
```

---

## 🛠️ Пошаговая загрузка файлов

Если у вас нет возможности создать архив, загружайте файлы по одному:

### Шаг 1: Подключение к VPS
```bash
ssh root@YOUR_VPS_IP
mkdir -p /opt/maptrack
cd /opt/maptrack
```

### Шаг 2: Создание файлов через nano

**1. bot.py:**
```bash
nano bot.py
# Скопируйте и вставьте содержимое bot.py
# Сохраните: Ctrl+X, Y, Enter
```

**2. metrics.py:**
```bash
nano metrics.py
# Скопируйте и вставьте содержимое metrics.py
```

**3. bot_runner.py:**
```bash
nano bot_runner.py
# Скопируйте и вставьте содержимое bot_runner.py
```

**4. requirements.txt:**
```bash
nano requirements.txt
# Добавьте:
selenium
requests
prometheus-client
```

**5. Dockerfile:**
```bash
nano Dockerfile
# Скопируйте содержимое Dockerfile
```

**6. docker-compose.yml:**
```bash
nano docker-compose.yml
# Скопируйте содержимое docker-compose.yml
```

**7. prometheus.yml:**
```bash
nano prometheus.yml
# Скопируйте содержимое prometheus.yml
```

**8. .env:**
```bash
nano .env
# Добавьте:
BOT_TOKEN=YOUR_ACTUAL_BOT_TOKEN
```

---

## 🚀 Автоматическая установка после загрузки

После загрузки всех файлов выполните:

```bash
# Установка Docker (если не установлен)
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Установка Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# Перезагрузка (для применения Docker прав)
sudo reboot

# После перезагрузки
ssh root@YOUR_VPS_IP
cd /opt/maptrack

# Настройка токена
nano .env  # Вставьте ваш BOT_TOKEN

# Запуск
docker-compose up -d --build

# Проверка
docker-compose ps
docker-compose logs -f bot
```

---

## 📋 Список файлов для загрузки

Убедитесь, что загрузили все необходимые файлы:

- ✅ `bot.py` - основной код бота
- ✅ `metrics.py` - система метрик
- ✅ `bot_runner.py` - автоперезапуск
- ✅ `requirements.txt` - зависимости Python
- ✅ `Dockerfile` - конфигурация контейнера
- ✅ `docker-compose.yml` - оркестрация сервисов
- ✅ `prometheus.yml` - конфигурация Prometheus
- ✅ `.env` - переменные окружения (с вашим токеном)

**Опциональные файлы:**
- `README.md` - документация
- `VPS_INSTALL.md` - инструкция по установке
- `view_metrics.py` - просмотр метрик

---

## 🔧 Настройка файрвола

```bash
# Установка UFW (если не установлен)
sudo apt install ufw

# Настройка правил
sudo ufw enable
sudo ufw allow ssh
sudo ufw allow 8000/tcp  # Метрики
sudo ufw allow 9090/tcp  # Prometheus
sudo ufw allow 3000/tcp  # Grafana

# Проверка
sudo ufw status
```

---

## 🌐 Проверка работы

После запуска проверьте доступность сервисов:

```bash
# Получение IP сервера
curl ifconfig.me

# Проверка портов
netstat -tlnp | grep -E ':(8000|9090|3000)'

# Тест метрик
curl http://localhost:8000/metrics
```

**В браузере:**
- `http://YOUR_VPS_IP:8000/metrics` - метрики бота
- `http://YOUR_VPS_IP:9090` - Prometheus
- `http://YOUR_VPS_IP:3000` - Grafana (admin/admin)

---

## 🆘 Решение проблем

### Проблема: Файлы не загружаются
```bash
# Проверьте права доступа
ls -la /opt/maptrack/

# Исправьте права
chmod +x /opt/maptrack/*.py
```

### Проблема: Docker не запускается
```bash
# Проверьте статус Docker
systemctl status docker

# Запустите Docker
systemctl start docker
systemctl enable docker
```

### Проблема: Порты заблокированы
```bash
# Проверьте файрвол
ufw status

# Откройте нужные порты
ufw allow 8000/tcp
ufw allow 9090/tcp
ufw allow 3000/tcp
```

### Проблема: Недостаточно прав
```bash
# Добавьте пользователя в группу docker
sudo usermod -aG docker $USER

# Перезайдите в систему
exit
ssh root@YOUR_VPS_IP
```

---

## ✅ Готово!

После успешной загрузки и настройки ваш бот будет работать 24/7 на VPS с полным мониторингом и автоматическим перезапуском.

**Следующие шаги:**
1. Протестируйте бота в Telegram
2. Настройте мониторинг в Grafana
3. Настройте резервное копирование
4. Добавьте SSL сертификат (опционально)